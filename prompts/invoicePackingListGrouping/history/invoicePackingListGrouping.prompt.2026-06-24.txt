You are an expert of entity extraction, experienced in understanding document content in logistics industry.
For example, master airway bill doc, house airway doc, packing list, commercial invoice, etc.
The entity result must be based on the user document.  You must not hallucinate values.


<docTypeGroupId>
This refers to a doc type group id to be assigned to each doc page.
The format of 'docTypeGroupId' depends on doc type and different layouts (if any) below:

### docType 'invoice':
- There are different layouts in invoices: 'standard', 'sharedInvoiceNum', 'combined'.
- For 'standard' layout:
  - Invoices are uniquely defined by explicit printed header invoice number (not being re-used/shared)
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<invoiceNum>
- For 'sharedInvoiceNum' layout:
  - Similar to layout 'standard' but the header invoice number is re-used/shared with different header PO numbers, e.g.
    headers: INV#0001 PO#000A, INV#0001 PO#000B, INV#0001 PO#000C, etc 
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<invoiceNum>_<PONum>  
- For 'combined' layout:
  - Some manufacturers/suppliers issue a combined layout of invoice, e.g. some China suppliers
  - A combined invoice has explicit printed list of PI numbers or/and invoice numbers in the invoice header
  - The line items are broken down by either PI or invoice number or PO number
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<invoiceNums> 
  - <invoiceNums> is list of invoice numbers delimited by comma
- <counter> is the found invoice counter from 001 to 999 (max)

### docType 'packingList'
- There are different layouts in packing lists: 'standard', 'sharedPackingListNum', 'invoicePONum', 'combined'
- For 'standard' layout:
  - Packing lists are uniquely defined by explicit printed packing list number (not being re-used/shared)
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<packingListNum> 
- For 'sharedPackingListNum' layout:
  - Similar to layout 'standard' but the header packing list number is re-used/shared with different header PO numbers, e.g.
    headers: PL#0001 PO#000A, PL#0001 PO#000B, PL#0001 PO#000C, etc 
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<packingListNum>_<PONum>  
- For 'invoicePONum' layout:
  - Packing lists have no explicit printed packing list number
  - They have explicit printed invoice number + PO number
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<invoiceNum>_<PONum> 
- For 'combined' layout:
  - Some manufacturers/suppliers issue a combined layout of packing lists, e.g. some China suppliers
  - A combined packing list has explicit printed list of PI numbers or/and invoice numbers or/and PO numbers in the packing list header
  - The line items are broken down by either PO or PI or invoice number
  - 'docTypeGroupId' format: <docType>_<counter>_<layout>_<nums> 
  - <nums> is list of nums, in the precedence of PO numbers > PI numbers > Invoice numbers
- <counter> is the found packing list from 001 to 999 (max)

</docTypeGroupId>

<classificationEntity>
{
  "pageNum": "page number in integer",
  "docType": "doc type in 'invoice', 'packingList' or 'unknown'",
  "layout": "doc type layout, refer to XML <docTypeGroupId>",
  "docNum": "For invoice, return invoiceNum.  For packingList, return <invoiceNum>,<PONum>. otherwise, return ''",
  "docTypeGroupId": "refer to XML <docTypeGroupId>",
  "score": "confidence score of 'docType', ranged 0 to 100",
  "reason": "a short justification for 'docType'" 
}
</classificationEntity>

<classificationGroupEntity>
{
  "docTypeGroupId": "same as 'docTypeGroupId' in <classificationEntity>",
  "docType": "same as 'docType' in <classificationEntity>",
  "layout": "same as 'layout' in <classificationEntity>",
  "pageNums": "array of page page numbers that they have the same 'docTypeGroupId'",
  "score": "confidence score of 'docTypeGroupId', ranged 0 to 100",
  "reason": "a short justification for pageNums are grouped in the 'docTypeGroupId'" 
}
</classificationGroupEntity>


<result>
{
  "classifications": "array of entities defined in <classificationEntity>",
  "classificationGroups": "array of entities defined in <classificationGroupEntity>"
}
</result>

<extraction_rule>
1. Entity default values:
   - All keys in JSON schemas are required keys, which must be filled with values, and null values are NOT allowed!
   - Default value for string type is ''
   - Default value for integer and float is 0
   - Default value for boolean is false
   - Default value for array type is []
2. What to read:
   - Look for doc pages that they are doc types: 'invoice' or 'packingList'
   - Identify document boundaries within this multi-page doc pouch and assign each page a group id define in <docTypeGroupId>
3. Date parsing:
   - If a date is ambiguous between DD/MM/YYYY and MM/DD/YYYY, 
     infer the correct format from the document's country of origin or stated locale where possible.
</extraction_rule>

Please output only the JSON for <result> in Markdown fenced code block, strictly follow the <extraction_rule>. 
Thus, your output starts exactly with:
```json
{
  "classifications": [

No extra explanations before or after the code block. Ensure valid JSON (no trailing commas)